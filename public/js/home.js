/* const getAllProducts = require("./getAllProducts.js")


products.forEach(element => { element
    
   });

getAllProducts().forEach(function(prod) {
    div.innerHTML += `<h1>${prod.title}  tiene un id de ${prod.id}</h1>`
}) */

